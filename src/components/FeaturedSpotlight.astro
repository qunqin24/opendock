---
import type { Plugin } from '../lib/plugins';
import { categoryLabel, lampClass } from '../lib/plugins';
import { avatarUrl, compactNumber } from '../lib/utils';
import { localeFromPath, localePath, useTranslations } from '../lib/i18n';

interface Props {
  plugins: Plugin[];
}

const { plugins } = Astro.props;
const lang = localeFromPath(Astro.url.pathname);
const t = useTranslations(lang);
---

<div class="grid grid-cols-1 gap-4 md:grid-cols-2 lg:grid-cols-4">
  {
    plugins.map((p) => (
      <a
        href={localePath(lang, `/plugins/${p.slug}`)}
        class="group flex flex-col justify-between rounded-lg border border-border/80 bg-card p-5 transition-colors hover:border-foreground/25"
      >
        <div>
          {/* Identity: avatar + title + package name, status lamp on the right */}
          <div class="flex items-center gap-3">
            <div class="size-8 shrink-0 overflow-hidden rounded-lg border border-border/80 bg-secondary">
              {p.ownerAvatar ? (
                <img
                  src={avatarUrl(p.ownerAvatar, 32)}
                  alt={p.owner ?? 'author'}
                  width="32"
                  height="32"
                  class="size-full object-cover"
                  loading="lazy"
                  decoding="async"
                />
              ) : (
                <div class="flex size-full items-center justify-center font-mono text-xs text-foreground/70">
                  {(p.owner ?? p.name).charAt(0).toUpperCase()}
                </div>
              )}
            </div>

            <div class="min-w-0 flex-1">
              <h3 class="truncate text-sm font-medium tracking-tight text-foreground">
                {p.title}
              </h3>
              <p class="truncate font-mono text-[0.6875rem] text-muted-foreground/80">
                {p.name}
              </p>
            </div>

            <span
              class:list={['lamp shrink-0', lampClass[p.healthLevel]]}
              title={t(`health.tip.${p.healthLevel}` as const)}
            />
          </div>

          <p class="mt-3 line-clamp-2 min-h-10 text-xs leading-relaxed text-muted-foreground">
            {p.description ?? t('common.noDescription')}
          </p>
        </div>

        {/* Bottom stats row: category + stars + downloads */}
        <div class="mt-4 flex items-center justify-between border-t border-border/60 pt-3 font-mono text-xs text-muted-foreground">
          <span class="font-sans text-[0.6875rem]">{categoryLabel(p.category, lang)}</span>

          <span class="flex items-center gap-3">
            <span class="inline-flex items-center gap-1" title={t('common.stars')}>
              <svg viewBox="0 0 16 16" fill="currentColor" class="size-3.5 text-amber-500" aria-hidden="true">
                <path d="M8 .25a.75.75 0 0 1 .673.418l1.882 3.815 4.21.612a.75.75 0 0 1 .416 1.279l-3.046 2.97.719 4.192a.75.75 0 0 1-1.088.791L8 12.347l-3.766 1.98a.75.75 0 0 1-1.088-.79l.72-4.194L.818 6.374a.75.75 0 0 1 .416-1.28l4.21-.611L7.327.668A.75.75 0 0 1 8 .25Z" />
              </svg>
              <span class="tabular text-foreground">{compactNumber(p.stars)}</span>
            </span>

            <span class="inline-flex items-center gap-1" title={t('explorer.downloadsTitle')}>
              <svg viewBox="0 0 16 16" fill="currentColor" class="size-3.5 text-muted-foreground/70" aria-hidden="true">
                <path d="M7.25 1.75a.75.75 0 0 1 1.5 0v6.44l2.22-2.22a.75.75 0 1 1 1.06 1.06l-3.5 3.5a.75.75 0 0 1-1.06 0l-3.5-3.5a.75.75 0 0 1 1.06-1.06l2.22 2.22V1.75ZM2.5 11a.75.75 0 0 1 .75.75v1.5h9.5v-1.5a.75.75 0 0 1 1.5 0v2.25a.75.75 0 0 1-.75.75H2.5a.75.75 0 0 1-.75-.75v-2.25A.75.75 0 0 1 2.5 11Z" />
              </svg>
              <span class="tabular">{compactNumber(p.downloadsMonth)}</span>
            </span>
          </span>
        </div>
      </a>
    ))
  }
</div>
