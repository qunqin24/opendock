---
import CategoryDetail from '../../../views/CategoryDetail.astro';
import { categories } from '../../../lib/plugins';

export function getStaticPaths() {
  return categories.map((category) => ({ params: { id: category.id }, props: { category } }));
}
---

<CategoryDetail category={Astro.props.category} />
